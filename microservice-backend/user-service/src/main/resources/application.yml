spring:
    application:
        name: user-service
    data:
        mongodb:
            uri: mongodb+srv://demo:2001@mycluster.ndj34hk.mongodb.net/purely_users?retryWrites=true&w=majority&appName=mycluster
    jpa:
        hibernate:
            ddl-auto: update

server:
    port: 9050

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/