apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-depl" .Values.cart.label }}  
  labels:
    app: {{ .Values.cart.label }}
spec:
  replicas: {{ .Values.cart.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.cart.label }}
  template:
    metadata:
      labels:
        app: {{ .Values.cart.label }}
    spec:
      containers:
        - name: {{ printf "%s-container" .Values.cart.label }}
          image: {{ .Values.cart.image }}
          ports:
            - containerPort: {{ .Values.cart.containerPort }}
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.config.label }}
                  key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
            - name: SPRING_DATA_MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.label }}
                  key: SPRING_DATA_MONGODB_URI_CART
            - name: EUREKA_INSTANCE_HOSTNAME
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.config.label }}
                  key: EUREKA_INSTANCE_HOSTNAME
