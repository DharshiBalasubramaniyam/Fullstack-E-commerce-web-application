apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-depl" .Values.auth.label }}  
  labels:
    app: {{ .Values.auth.label }}
spec:
  replicas: {{ .Values.auth.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.auth.label }}
  template:
    metadata:
      labels:
        app: {{ .Values.auth.label }}
    spec:
      containers:
        - name: {{ printf "%s-container" .Values.auth.label }}
          image: {{ .Values.auth.image }}
          ports:
            - containerPort: {{ .Values.auth.containerPort }}
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.config.label }}
                  key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
            - name: SPRING_DATA_MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.label }}
                  key: SPRING_DATA_MONGODB_URI_AUTH
            - name: EUREKA_INSTANCE_HOSTNAME
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.config.label }}
                  key: EUREKA_INSTANCE_HOSTNAME
          resources:
            requests:
              cpu: {{ .Values.auth.requests.cpu }}
              memory: {{ .Values.auth.requests.memory }}
            limits:
              cpu: {{ .Values.auth.limits.cpu }}
              memory: {{ .Values.auth.limits.memory }}
