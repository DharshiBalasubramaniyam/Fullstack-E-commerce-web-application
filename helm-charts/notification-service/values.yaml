notification:
  label: notification
  replicas: 1
  containerPort: 9000
  image: default
  service:
    type: ClusterIP
    port: 9000

secret:
  label: notification-secret
  SPRING_MAIL_USERNAME: 
  SPRING_MAIL_PASSWORD: 

config:
  label: notification-config-map
  EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://registry-svc:80/eureka/
  EUREKA_INSTANCE_HOSTNAME: notification-svc