name: CI/CD Pipeline Category Service

on: 
   push:
      branches:
         - docker
      paths:
         - 'microservice-backend/category-service/**'

jobs:
   containerize-category-service:
      runs-on: ubuntu-latest

      steps:
         - name: Checkout code  
           uses: actions/checkout@v4

         - name: Set up Java
           uses: actions/setup-java@v3
           with:
               java-version: '17'
               distribution: 'temurin'

         - name: build and push the docker image to dockerhub
           run: |
               docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

               docker build -t ${{ secrets.DOCKER_USERNAME }}/purely-category-service:latest ./microservice-backend/category-service
               docker push ${{ secrets.DOCKER_USERNAME }}/purely-category-service:latest
